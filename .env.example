# PostgreSQL / Supabase connection string (required).
DATABASE_URL="postgresql://user:password@localhost:5432/soma?schema=public"

# Optional: enables the AI sommelier explanation layer.
# When unset, SOMA falls back to its built-in deterministic Taste Match Engine.
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"
